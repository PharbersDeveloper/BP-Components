{{#choose-group
		type="radio" 
		vertical=vertical 
		onChange=(action 'chooseItem') as |choose|}}
	{{#each model as |radio|}}
		{{choose.item type="default" radio=radio}}
	{{/each}}
{{/choose-group}}
{{outlet}}