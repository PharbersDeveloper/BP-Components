import Component from '@ember/component';
import layout from '../../templates/components/bp-card/header';

export default Component.extend({
	layout,
	tagName: 'p',
	classNames: ['bp-card-header']
});
