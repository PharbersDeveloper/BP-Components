import Component from '@ember/component';
import layout from '../templates/components/bp-progress';

export default Component.extend({
	layout,
	classNames: ['bp-progress'],
	bpProgressBar: 'bp-progress/bar'
});
