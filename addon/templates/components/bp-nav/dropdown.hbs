<div {{action 'changeShowProperty'}} class="dd-title {{if titleActive 'title-active'}}">
	{{#if icon}}
		{{bp-icon icon=icon color=iconColor size=iconSize}}
	{{/if}}
	{{title}}
</div>
<ul class="bp-nav-dd-ul">
	{{yield
			(hash
				item=(component ddItem onClick=(action 'chooseDdList'))
				link-to=(component ddLinkTo)
			)
		}}
</ul>