{{#if title}}
	<p class="bp-radio-title">{{title}}</p>
{{/if}}
{{#choose-group
	type="checkbox" 
	vertical=vertical 
	value=curValues
	onChange=(action (mut curValues)) as |choose|}}
	{{#each checkboxs as |radio|}}
		{{choose.item radio=radio}}
	{{/each}}
{{/choose-group}}