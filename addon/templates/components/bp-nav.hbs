{{!-- {{yield
	(hash 
		item=(component bpNavItem )
		link-to=(component bpNavLinkTo)
		dropdown=(component bpNavDropdown)	
	)	
}} --}}
{{yield
	(hash 
		link-to=(component bpNavLinkTo)
		dropdown=(component bpNavDropdown)	
	)	
}}